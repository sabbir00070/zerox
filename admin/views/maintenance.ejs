<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Maintenance</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>
<body class="bg-gradient-to-br from-gray-900 to-gray-800 min-h-screen flex items-center justify-center font-sans text-gray-100 p-4">

  <div class="w-full max-w-md bg-gray-900/90 backdrop-blur-md rounded-2xl shadow-2xl p-6">
    <div class="mb-6 text-center">
      <h1 class="text-3xl font-bold text-yellow-500 mb-2">Maintenance Mode</h1>
      <p class="text-gray-400 text-sm">Control site maintenance settings</p>
    </div>
    <form action="/admin/maintenance" method="POST" class="space-y-5">
      <div class="flex items-center justify-between">
        <span class="text-gray-300 font-medium">Enabled</span>
        <label class="relative inline-flex items-center cursor-pointer">
          <input type="checkbox" name="enabled" <%= maintenance?.enabled ? "checked" : "" %> class="sr-only peer">
          <div class="w-14 h-7 bg-gray-700 rounded-full peer peer-checked:bg-yellow-500 transition-all duration-300"></div>
          <div class="absolute left-1 top-1 w-5 h-5 bg-white rounded-full peer-checked:translate-x-7 transition-all duration-300 shadow"></div>
        </label>
      </div>
      <div>
        <label class="block text-gray-300 mb-1 font-medium">Title</label>
        <input name="title" value="<%= maintenance?.title || '' %>" 
          class="w-full p-3 rounded-xl border border-gray-700 bg-gray-800 text-gray-100 focus:ring-2 focus:ring-yellow-500 focus:border-transparent transition shadow-inner">
      </div>
      <div>
        <label class="block text-gray-300 mb-1 font-medium">Message</label>
        <textarea name="message" rows="3" 
          class="w-full p-3 rounded-xl border border-gray-700 bg-gray-800 text-gray-100 focus:ring-2 focus:ring-yellow-500 focus:border-transparent transition shadow-inner"><%= maintenance?.message || '' %></textarea>
      </div>
      <div>
        <label class="block text-gray-300 mb-1 font-medium">Start Time</label>
        <input type="datetime-local" name="start_time" 
          value="<%= maintenance?.start_time ? new Date(maintenance.start_time).toISOString().slice(0,16) : '' %>" 
          class="w-full p-3 rounded-xl border border-gray-700 bg-gray-800 text-gray-100 focus:ring-2 focus:ring-yellow-500 focus:border-transparent transition shadow-inner">
      </div>
      <div>
        <label class="block text-gray-300 mb-1 font-medium">End Time</label>
        <input type="datetime-local" name="end_time" 
          value="<%= maintenance?.end_time ? new Date(maintenance.end_time).toISOString().slice(0,16) : '' %>" 
          class="w-full p-3 rounded-xl border border-gray-700 bg-gray-800 text-gray-100 focus:ring-2 focus:ring-yellow-500 focus:border-transparent transition shadow-inner">
      </div>
      <button type="submit" 
        class="w-full bg-yellow-500 hover:bg-yellow-600 shadow-lg shadow-yellow-500/50 text-gray-900 font-semibold py-3 rounded-2xl transition-transform hover:scale-105">
        Save Settings
      </button>

    </form>
  </div>

</body>
</html>