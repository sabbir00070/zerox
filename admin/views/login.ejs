<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Zerox Admin Login</title>
<script src="https://cdn.tailwindcss.com"></script>
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
  body {
    font-family: 'Poppins', sans-serif;
    background-color: #0a0a0a;
  }

  .blood-bg::after {
    content: '';
    position: fixed;
    top:0; left:0;
    width:100%; height:100%;
    background:
      radial-gradient(circle at 20% 20%, rgba(255,0,0,0.2), transparent 70%),
      radial-gradient(circle at 80% 80%, rgba(139,0,0,0.2), transparent 70%),
      radial-gradient(circle at 50% 50%, rgba(255,0,0,0.15), transparent 70%);
    pointer-events: none;
    z-index: 0;
  }

  .drop {
    position: absolute;
    width: 6px;
    height: 12px;
    background: linear-gradient(180deg, #ff0000 0%, #8b0000 100%);
    border-radius: 50%;
    animation: drip 3s linear infinite;
    opacity: 0;
  }

  @keyframes drip {
    0% { transform: translateY(-50px) rotate(0deg); opacity: 0; }
    50% { opacity: 1; }
    100% { transform: translateY(250px) rotate(180deg); opacity: 0; }
  }

  input, button {
    font-family: 'Poppins', sans-serif;
  }
</style>
</head>
<body class="blood-bg flex items-center justify-center min-h-screen px-4 relative">
  <% for(let i=0;i<12;i++){ %>
  <div class="drop" style="left:<%= i*8 %>%; animation-delay: <%= Math.random()*3 %>s;"></div>
  <% } %>
<div class="relative z-10 w-full max-w-md">
<div class="bg-gray-10 text-red-100 rounded-3xl shadow-2xl p-10 border-2 border-red-700">
<div class="mb-6 text-center">
  <img src="https://i.ibb.co/tpm4smXJ/db10df5203.jpg" 
       alt="Admin Icon" 
       class="mx-auto w-24 h-24 sm:w-28 sm:h-28 md:w-32 md:h-32 rounded-full shadow-lg">
</div>
  <h1 class="text-3xl sm:text-4xl font-bold mb-2 text-red-500 tracking-wide text-center">
    Zerox Admin Login
  </h1>
  <p class="text-red-300 mb-6 text-sm sm:text-base leading-relaxed text-center">
    Enter your credentials to access the admin panel.
  </p>
  <% if (typeof error !== 'undefined') { %>
    <div class="bg-red-800 border border-red-600 text-red-100 px-4 py-2 rounded mb-6 font-medium">
      <%= error %>
    </div>
  <% } %>
  <form action="/admin/login" method="POST" class="space-y-5">
    <div>
      <label class="block text-red-300 font-medium mb-1">Username</label>
      <input name="username" placeholder="USERNAME"
             class="w-full px-5 py-3 border border-red-600 rounded-xl shadow-sm bg-gray-800 text-white focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-red-500 transition" required>
    </div>
    <div>
      <label class="block text-red-300 font-medium mb-1">Password</label>
      <input type="password" name="password" placeholder="PASSWORD"
             class="w-full px-5 py-3 border border-red-600 rounded-xl shadow-sm bg-gray-800 text-white focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-red-500 transition" required>
    </div>

    <button type="submit"
            class="w-full py-3 bg-red-700 hover:bg-red-600 text-white font-semibold rounded-xl shadow-lg transition transform hover:scale-105">
      Login
    </button>
  </form>
  <div class="mt-6 text-red-500 text-xs sm:text-sm tracking-widest text-center">
    ZEROX ADMIN PANEL • &copy; <%= new Date().getFullYear() %>
  </div>
</div>
</div>
</body>
</html>